# URL server-only để SSR gọi tutor-api. Không có default production.
API_BASE_URL=http://localhost:3000/api/v1
# Host canonical public, không kèm dấu / cuối.
NEXT_PUBLIC_SITE_URL=http://localhost:3001
# Chỉ dùng để review UI local khi tutor-api chưa chạy; production luôn bỏ qua.
MARKET_DEV_FIXTURES=false
